let word = "Yakubu";
let reverse = "";

    for(let count = word.length() - 1; count >= 0; count --){
       reverse =+ word.charAt(count);
//       console.log(reverse); 
    }
        console.log(reverse);
